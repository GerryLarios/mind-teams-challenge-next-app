import { styled } from '@mui/material';

export default styled('div')({
  alignSelf: 'center',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  maxWidth: 423,
  minHeight: '100%',
});
